Use an organization-scoped LedgerBill API key from your LedgerBill Developers page.

This extension stores the key in VS Code Secret Storage and uses it only for customer-facing API calls such as usage previews and usage reporting.
